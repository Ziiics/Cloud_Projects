Project 2 password Zics23951334!

Get into EC2 instance
    ssh -i "project2-keypair.pem" ubuntu@18.207.179.105
To get to venv
    source .venv/bin/activate
Then to deactivate
    deactivate 2>/dev/null || true

To get to the database
    mysql -h project2-database.cgpcgg00cq2s.us-east-1.rds.amazonaws.com -u admin -p

Database information
    Databse Name: purpose
    Table Name: visit_purpose

Nginx
    To find nginx file: nginx -V 2>&1 | grep --color=always -oP '(?<=--conf-path=)[^ ]+'

For Mac, to be able to run docker command, have to add docker command to your path manually
    export PATH="$PATH:/Applications/Docker.app/Contents/Resources/bin/"

On EC2, to get user
    whoami
Then to ensure the current user are able to run dockersudo 
    usermod -aG docker ubuntu
        Then just restart it, check it with 
            docker ps

