// Used by AWS CodeBuild to tell AWS how ot build your application, as install dependencies, run code

/*
Think of it as a build instruction file.

version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies...
      - npm install

  build:
    commands:
      - echo Building the application...
      - npm run build

  post_build:
    commands:
      - echo Build completed on `date`

artifacts:
  files:
    - '**/*'
  discard-paths: yes
*/